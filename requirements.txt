# The ground station is the only Python component with dependencies.
# Kept as a pointer so this file cannot drift from the real list again
# (it previously omitted dearpygui, which station.py imports at startup).
-r driver/requirements.txt
